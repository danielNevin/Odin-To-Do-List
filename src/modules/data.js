import 'lodash';

window.taskArray = [];
window.projectArray = [];

function createTask() {
  const task = new task(title, priority, dueDate, project);
  taskArray.push(task);
}

function createProject(projectName) {
  projectArray.push[projectName];
}

export { createProject };